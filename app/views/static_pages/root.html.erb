<script id="bootstrap-current-user" type="text/javascript">
  <% if logged_in? %>
  	window.currentUser = <%= render("api/users/user.json.jbuilder",
  		user: current_user).html_safe %>
  <% end %>
  window.session_form_background="<%= asset_path('session_form_background.jpg')%>"
  window.logo="<%= asset_path('logo.jpg')%>"
  window.searchbutton="<%= asset_path('searchbutton.png')%>"
</script>

<main id="root">
</main>
